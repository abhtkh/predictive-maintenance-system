# This is an example environment file.
# Copy this to a new file named ".env" and fill in your actual credentials.

# --- PostgreSQL Database Credentials ---
POSTGRES_USER=pdm_user
POSTGRES_PASSWORD=pdm_password
POSTGRES_DB=pdm_database
POSTGRES_SERVER=localhost # Use 'postgres' if running the backend in Docker

# --- Redis Configuration ---
REDIS_HOST=localhost # Use 'redis' if running the backend in Docker
REDIS_PORT=6379

# --- JWT Secret ---
# Generate a strong secret with: openssl rand -hex 32
JWT_SECRET_KEY=YOUR_SUPER_SECRET_RANDOM_KEY_HERE

# --- SMTP Email Alerting Credentials ---
SMTP_ENABLED=true
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your_16_character_gmail_app_password
SMTP_SENDER_EMAIL=pdm-alerts@your-company.com